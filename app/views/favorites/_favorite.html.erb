<%= form_for(current_user.favorites.build,html:{class: "inline-block"},remote: true) do |f| %>
  <%= hidden_field_tag :micropost_id, @micropost.id %>
  <%= f.submit "favo", class: "btn btn-primary" %>
<% end %>